name: Strip Source Footer
on:
  workflow_dispatch:

jobs:
  strip:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run strip script
        run: python3 scripts/strip-source-footer.py
