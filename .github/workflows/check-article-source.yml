name: Check Article Source
on:
  workflow_dispatch:
jobs:
  check:
    runs-on: ubuntu-latest
    env:
      SANITY_TOKEN: ${{ secrets.SANITY_TOKEN }}
    steps:
      - uses: actions/checkout@v4
      - name: Read news status from Sanity
        run: python3 scripts/read-news-status.py
