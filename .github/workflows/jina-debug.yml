name: jina-debug
on:
  workflow_dispatch:
    inputs:
      dummy:
        required: false
jobs:
  guard:
    runs-on: ubuntu-latest
    steps:
      - run: echo "guard"
  run:
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch'
    steps:
      - uses: actions/checkout@v4
      - name: Debug Jina
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
        run: python3 scripts/jina_debug.py
