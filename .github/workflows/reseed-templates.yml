name: Reseed Outreach Templates
on:
  workflow_dispatch:
jobs:
  reseed:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Reseed templates via Sanity API
        run: python3 scripts/reseed-templates.py
