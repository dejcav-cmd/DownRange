name: Fix Article Images
on:
  workflow_dispatch:
permissions:
  contents: write
jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - run: python scripts/fix-article-images.py
        env:
          SANITY_TOKEN: ${{ secrets.SANITY_TOKEN }}
      - run: |
          git config user.name "DownRange Bot"
          git config user.email "dj@downrangeco.com"
          git add -f scripts/article-fix-result.txt
          git commit -m "chore: article image fix result" || echo "nothing"
          git push
